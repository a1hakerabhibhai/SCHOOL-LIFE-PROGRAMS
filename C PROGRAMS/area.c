#include<stdio.h>
int main()
{
    int l, b;
    printf("Enter the length of square = ");
    scanf("%d", &l);
    printf("Enter the breadth of square = ");
    scanf("%d", &b);
    printf("The area of square is = %d", l*b);
    return 0;
}
